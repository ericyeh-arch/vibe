# Vibe Code GitHub Pages Interactive Guide

這是一個由 `vibe-code-github-pages-ppt.pptx` 轉出的互動式靜態網頁。

## 檔案

- `index.html`：主頁面
- `styles.css`：響應式版面與視覺樣式
- `script.js`：流程切換、FAQ 收合、部署檢核進度
- `assets/ppt-image-1.png`：PPT 內抽出的主視覺圖片

## 使用方式

直接用瀏覽器開啟 `index.html` 即可。本專案不需要後端或資料庫，可直接上傳到 GitHub Pages。
